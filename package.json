{
  "name": "garage37",
  "version": "1.0.0",
  "private": true,
  "description": "Build/minifikacja motywu WordPress Garage 37",
  "scripts": {
    "build": "node build.mjs",
    "package": "node build.mjs && bash package.sh"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
